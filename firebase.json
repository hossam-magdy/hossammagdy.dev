{
  "functions": {
    "source": ".",
    "predeploy": [
      "npm --prefix \"$PROJECT_DIR\" install", // ensure node_modules is onstalled
      "npm --prefix \"$PROJECT_DIR\" run build" // build/compile nextJs
    ]
  }
}
